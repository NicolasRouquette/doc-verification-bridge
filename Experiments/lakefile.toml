name = "experiments"
version = "0.1.0"
keywords = ["experiments", "benchmarking", "multi-project"]
defaultTargets = ["Experiments","experiments"]
leanVersion = "leanprover/lean4"

[[lean_lib]]
name = "Experiments"

[[lean_exe]]
name = "experiments"
root = "Main"
supportInterpreter = true

# Note: Experiments module is intentionally independent of DocVerificationBridge
# It orchestrates by invoking unified-doc as a subprocess, allowing it to work
# across multiple Lean versions without version-specific dependencies.
