.lake
tests

# Generated documentation sites are published to the orphan `pages` branch,
# never committed to main (see scripts/publish-pages.sh). Guard against a
# pipeline ever depositing them at the repo root and churning main's history.
/docs
/.backups
